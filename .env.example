# A single hostname, with no https://, path or trailing slash.
DOMAIN=example.com

# Optional image tag; use a release tag if you want to keep rollback images.
IMAGE_TAG=local

# Only used with compose.proxy.yaml (existing host Nginx / panel).
APP_PORT=3000
