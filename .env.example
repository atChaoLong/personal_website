# Optional image tag; use a release tag if you want to keep rollback images.
IMAGE_TAG=local

# Host Nginx forwards to this loopback port; assign each project a unique port.
# Keep deploy/nginx/atchaolong.conf's proxy_pass in sync when changing this.
APP_PORT=3001
